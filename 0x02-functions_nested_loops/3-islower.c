#include "main.h"

/**
 * _islower-> checks if the character is
 * lowercase or upper
 * @c: argument for character
 * Return: return 1 or 0 depend on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
