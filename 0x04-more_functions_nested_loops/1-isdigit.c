#include "main.h"

/**
 * _isdigit-> is tocheck weather it is digit or not
 * @c: argument fro function
 * Retrurn: succes
 * Return: succes
 *
 */
int _isdigit(int c)
{
	if (c <= 0 && c >= 9)
		return (1);
	return (0);
}
