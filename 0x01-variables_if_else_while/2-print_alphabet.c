#include <stdio.h>

/**
 * main- the main function has a block of code
 * that display lower case character
 * Return: always return 0
*/
int main(void)
{
	char ch = 'a';

	for (ch = a; ch <= z; ch++)
	{
		putchar("%c\n", ch);
	}
	return (0);
}
